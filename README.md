# A simple Node.js + express server
Simple server who lets you to test push notifications in Android with Google Cloud Messaging.  
It will only send an example notification throw  [GCM](https://developers.google.com/cloud-messaging/ "Google Cloud Messaging") key and sending a push notification with that key.

